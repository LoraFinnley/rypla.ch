<!DOCTYPE html>
<html lang="de">
  <head>
    <!-- site meta -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      <% if (locals.siteTitle) { %> <%= siteTitle %> <% } else { %> HTML Site
      Title<% } %>
    </title>

    <!-- Favicon -->
    <link rel="icon" href="/images/general/favicon.ico" type="image/png" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Serif+Georgian:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Serif+Georgian:wght@100..900&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap"
      rel="stylesheet"
    />
    <!-- stylesheets -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/styles.css?v=0.9.2" />

    <!-- Google reCAPTCHA -->
    <script src="https://www.google.com/recaptcha/api.js"></script>
  </head>
  <body>
    <header>
      <!-- Brand Logo-->
      <img
        src="/images/general/logo.png"
        class="brand_logo"
        alt="Ryser Plan GmbH Logo"
      />

      <!-- Navigation -->
      <nav class="navbar container navbar-expand-lg">
        <div class="container-fluid">
          <!-- Hamburger Menu for Mobile -->
          <button
            class="navbar-toggler"
            type="button"
            data-bs-togg
            -äölkj
            hgfle="collapse"
            data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <!-- Navigation Links -->
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
              <a class="nav-link" href="/#services_section">angebot</a>
              <a class="nav-link" href="/#about_section">über rypla</a>
              <a class="nav-link" href="/#contact_section">kontakt</a>
            </div>
          </div>
        </div>
      </nav>
    </header>
  </body>
</html>
